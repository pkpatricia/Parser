<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mypackage.nagla">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/naglaAppTheme">
        <activity
            android:name="com.example.mypackage.nagla.Splash"
            android:screenOrientation="portrait"
            android:theme="@style/naglaAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.mypackage.nagla.HomeScreenActivity"
            android:label="@string/title_activity_home_screen"
            android:theme="@style/naglaAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.mypackage.nagla.CreateGroceryListActivity"
            android:parentActivityName="com.example.mypackage.nagla.HomeScreenActivity" />
        <activity
            android:name="com.shermansthill.com.example.mypackage.nagla.GroceryListActivity"
            android:parentActivityName="com.example.mypackage.nagla.HomeScreenActivity" />

        <activity
            android:name="com.example.mypackage.nagla.StoreMapsActivity"
            android:label="@string/title_activity_store_maps"
            android:theme="@style/naglaAppTheme.NoActionBar" />


        <activity android:name="com.example.mypackage.nagla.AddGroceryListItemsActivity"
            android:parentActivityName="com.example.mypackage.nagla.GroceryListActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>